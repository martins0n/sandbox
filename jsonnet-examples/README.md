# Examples

```bash

jsonnet docker-compose.template.jsonnet --ext-str imageFlag=false > docker-compose.yaml

jsonnet docker-compose.template.jsonnet --ext-str imageFlag=true > docker-compose.yaml

```